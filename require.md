- # 中国象棋 UCI 客户端界面 - 项目需求规格说明书 (PRD)

  ## 1. 项目概述

  ### 1.1 项目背景与初衷

  Linux 平台长期缺少好用的开源中国象棋综合分析软件（涵盖棋谱、引擎、开局库、云库、连线等功能）。本项目旨在填补这一空白，开发一款跨平台、功能全面的中国象棋 UCI 可视化客户端界面，且全程开源，代码托管于 GitHub。

  ### 1.2 远期目标

  代码需具备跨架构、跨平台编译能力，可在 **龙芯架构** 及 **国产 Linux 操作系统** 上成功编译并正确运行。

  ### 1.3 跨平台策略

  除 Linux 外，同时支持 **Windows** 平台。图形界面框架选定 **wxWidgets 3.2.11**，利用其跨平台特性实现全部界面功能。要求支持高分屏。

  ## 2. 主界面与窗口布局

  ### 2.1 主窗口结构

  - **启动方式**：双击程序直接打开。
  - **初始界面**：打开后仅显示一个中国象棋棋盘（作为核心视觉元素）。
  - **顶栏**：窗口标题栏下方包含两行：
    - 第一行：**菜单栏**
    - 第二行：**工具栏**（由图标+文字按钮组成）
  - **底栏**：主窗口底部设有**状态栏**。
    - **显示内容**：招法深度、局面分数、运算时间、NPS、未吃子步数。
    - **自定义**：支持用户自定义状态栏显示项及显示顺序。

  ### 2.2 停靠子窗口系统

  界面采用类似 Keil 软件的可停靠（Dock）子窗口系统。主窗口固定保留顶部菜单栏和工具栏，其余功能面板均为可停靠、吸附、标签化分组的子窗口。

  #### 2.2.1 默认布局

  - **左上角**：棋盘子窗口
  - **右上角**：引擎程序子窗口
  - **左下角**：主数据库、辅数据库、在线数据库（三个窗口整合在同一区域，标签页切换）
  - **右下角**：形势分析窗口（上）、棋谱记录窗口（下）
  - **外部对局连接窗口**：默认关闭或浮空，不参与默认停靠布局。

  #### 2.2.2 各子窗口功能明细

  **1. 棋盘子窗口**

  - **棋盘渲染**：绘制棋盘、棋子，支持预留换肤接口，内置 1-2 套皮肤。
  - **信息展示**：显示双方玩家名称、当前轮到哪一方行棋、双方各自剩余对局时间。
  - **引擎走法提示**：引擎运行时，在棋盘上显示引擎给出的前两步候选招法箭头。
  - **翻转功能**：提供棋盘红黑视角翻转功能。
  - **交互**：鼠标点击选子、再点击目标格落子（非拖拽）。
  - **提示**：内含跳转设置的按钮。

  **2. 主数据库（开局库）窗口**

  - **功能**：展示开局库招法，对局前期优先调用库招代替引擎计算。
  - **格式支持**：支持 OBK 格式，兼容鹏飞象棋等棋库格式。
  - **设置入口**：其参数配置位于菜单栏【数据库】菜单的数据库设置中（如是否启用开局库）。

  **3. 辅数据库窗口**

  - **功能**：除指定主数据库外的其它棋库均作为辅数据库，支持加载、查看多个 OBK 等格式库文件，用于多库对照参考。

  **4. 在线数据库（云库）窗口**

  - **功能**：展示云库招法，支持开启/关闭。
  - **设置**：提供云库设置，可配置请求链接、请求参数等。

  **5. 棋谱记录窗口**

  - **功能**：显示打开或新建棋谱的完整招法列表。开局为初始局面，其后按顺序罗列每一步。
  - **招法信息**：每条招法可附带引擎评估分数、搜索深度及用户注释。
  - **同步跳转**：点击棋谱中任意一步，棋盘子窗口将同步跳转至该步完成后的局面。

  **6. 引擎程序子窗口**

  - **功能**：展示所有已启用引擎的运行状态、原始通信日志（STDOUT）及关键指标。
  - **指标展示**：标准化显示 NPS、候选招法、搜索深度等。
  - **配置项**：可为每条引擎配置程序路径、哈希大小、使用线程数，并分配候选招法的提示箭头颜色。
  - **模式切换**：内含设置按钮，可跳转至引擎完整设置界面（思考设置、引擎管理、引擎参数、多引擎策略）。
  - **防卡机制**：引擎通信日志输出必须有防卡死处理。

  **7. 形势分析窗口**

  - **功能**：跟随当前棋谱步数，实时显示红方/黑方形势得分。
  - **曲线图**：绘制实时形势曲线图，横轴为棋谱步数，纵轴为局面评分，用于分析形势优劣变化。

  **8. 外部对局连接窗口**

  - **功能**：用于外部对局连线功能，提供目标窗口定位、棋盘基准标定、识别参数调节。
  - **设置跳转**：内含按钮可跳转至完整的外部对局设置界面。

  ## 3. 菜单栏功能详述

  ### 3.1 文件菜单

  操作对象均为**棋谱文件**，包含：新建棋谱、打开棋谱、导出棋谱、保存棋谱、另存为等。

  ### 3.2 编辑菜单

  操作对象为**当前局面**，并同步影响棋谱内容，但不直接操作棋谱文件。

  - **导航**：后退一招、后退至开局、前进一招、前进至中局、播放棋谱。
  - **修改**：悔棋、编辑局面。
  - **剪贴板**：复制局面、粘贴局面、输入局面码。

  ### 3.3 数据库菜单

  用于管理开局库、本地谱库和云库。

  - **核心管理**：棋库的导入、导出、入库。
  - **设置**：管理对局中是否启用对应棋库，配置云库访问地址等。

  ### 3.4 引擎程序菜单

  - **核心控制**：引擎执红、引擎执黑、立即出招、强制变招。
  - **分析**：深度分析。
  - **配置入口**：引擎设置（打开完整的引擎设置弹窗，包含思考设置、引擎管理、引擎参数、多引擎策略四个标签页）。

  ### 3.5 外部对局菜单

  - **功能**：实现外部窗口棋盘识别与捕获，解析当前局面。
  - **行棋操作**：通过发送窗口消息或模拟鼠标点击，控制目标外部窗口完成行棋。

  ### 3.6 窗口布局菜单

  - **布局管理**：统一管理所有可停靠子窗口的显示、隐藏、布局调整。
  - **恢复默认**：一键恢复出厂默认的停靠布局，防止用户自定义布局混乱。

  ### 3.7 关于菜单

  - **信息展示**：包含项目 GitHub 地址、开源许可协议、贡献者信息。
  - **预留入口**：帮助文档入口（暂不实现具体内容）。

  ## 4. 工具栏

  - **样式**：每个按钮由“图标 + 下方文字说明”组成，降低新手学习成本。
  - **功能来源**：所有工具栏按钮均源自菜单栏已有功能项。
  - **自定义**：支持用户自定义工具栏上显示的按钮及其排列顺序。

  ## 5. 核心功能模块详述

  ### 5.1 棋盘与规则校验

  - **走棋方式**：鼠标点击选子，再点击落子格。
  - **规则内置**：程序须内置完整的中国象棋规则逻辑。
    - **走法校验**：校验所有棋子的合法走法（如卒未过河只能前进、过河可平移，不能后退；士、象的限定走法等）。
    - **局面判断**：能正确识别将军、将死、困毙、被将后未应将等所有局面状态。
  - **非法招处理**：当试图走出非法招法时，棋盘“楚河汉界”区域用红色文字显示“非法招数”，该步不会写入棋谱。

  ### 5.2 悔棋

  - **逻辑**：正常情况下，悔棋一次回退两步（双方各一步），并删除这两步。
  - **安全机制**：若在棋谱中间步骤执行悔棋，会因破坏后续招法而弹出确认框，询问“是否销毁后续棋谱并重走？”。确认则删除，取消则无操作。

  ### 5.3 引擎系统

  引擎内核与图形界面逻辑解耦。GUI 负责下发局面和接收数据。引擎是程序外部的下棋程序，而本项目只是个界面。

  #### 5.3.1 引擎管理

  - **配置信息**：每条引擎需保存名称、通信协议（UCI）、作者、可执行文件路径、启动命令行参数。
  - **选项读取**：启动引擎后，通过 UCI 协议交互，读取并显示引擎支持的全部选项，区分标准选项和非标准自定义选项。
  - **单引擎参数**：可单独设置进程优先级、是否开启后台思考、引擎执方颜色、棋盘上招法箭头颜色、分数显示模式（几分制/红方视角/原始）、是否显示哈希命中信息等。

  #### 5.3.2 思考设置（5种工作模式）

  1. **引擎执红**：引擎自动行棋执红方。
  2. **引擎执黑**：引擎自动行棋执黑方。
  3. **引擎对战**：引擎同时操控红黑双方自动对弈。
  4. **仅分析**：只分析局面给出分数和招法，不自动走棋。
  5. **休眠**：引擎停止一切工作。

  **思考计时模式（单选）**：

  - **固定时间/深度**：每步固定时间（秒）或固定搜索深度（层），可启用单步限时。
  - **固定节点数**：每步搜索固定节点数。
  - **限时制**：模拟正式比赛计时。
    - 设置局时、步时、读秒、每步加时、防超时缓冲。
    - 前N回合强制固定步时开关。
    - 对局消耗指定分钟后切换固定步时开关。
  - **分数步时**：动态分配思考时间，根据当前局面评分，优势时用时短，劣势时用时长。提供评分与时间的映射表进行配置。

  **思考方案管理**：支持保存、载入、更新、删除多套思考方案，方案列表清晰展示并可排序。

  #### 5.3.3 多引擎策略（最多支持1-5个引擎）

  - **主副引擎**：最先启动的引擎为“主引擎”，负责默认出招，其余为“副引擎”。
  - **触发规则**：以下两条件可独立开启。
    1. **副引擎高分接管**：任意副引擎的评估分数超过设定阈值，则采纳该副引擎的走法。
    2. **加权对比接管**：副引擎分数 × 倍率 > (主引擎分数 + 分数门槛)，条件成立则副引擎接管出招权。

  ### 5.4 音效系统

  - **触发事件**：
    1. **选棋**：鼠标点击选中己方合法棋子时。
    2. **走棋**：完成一次普通移动落子时。
    3. **吃子**：落子并吃掉对方棋子时。
    4. **将军**：走棋后形成将军局面时。
    5. **将死/困毙**：对局结束，形成杀棋时。
    6. **悔棋**：执行悔棋操作时。
  - **引擎自动行棋**：引擎走子、吃子、将军、杀棋时同样触发上述对应音效。
  - **逻辑要求**：音效必须异步播放，绝不能阻塞 UI 和引擎通信。短时间连续事件支持截断重放，防止混音嘈杂。
  - **用户设置**：
    - 总音量滑块。
    - 各音效独立试听预览和开关。
    - 全局静音开关。
    - 预留自定义音效文件替换接口。

  ### 5.5 外部对局（连线）模块

  通过图像识别技术捕获第三方象棋软件窗口棋盘，实现自动分析与出招。

  - **方案管理**：可新建、编辑、删除、导入导出多套识别方案，适应不同象棋软件。
  - **核心设置**：
    - **棋盘状态**：设定我方先行或对方先行。
    - **棋盘标定**：通过拖拽靶心到棋盘左下角“车”的中心点，完成坐标基准标定。
    - **模糊配置**：提供图像识别容错参数（像素偏移、色差百分比），提高识别兼容性。
    - **行棋方式**：可选“发送窗口消息”或“模拟鼠标硬件操作”来执行外部窗口落子。
    - **自动点击**：支持设置找图周期、查找范围等，实现自动化操作。
  - **自动保存**：可开启“连线时自动保存棋谱”功能。

  ### 5.6 棋谱系统

  - **支持格式**：`PGN`、`CHE` (QQ象棋)、`XQF` (象棋演播室)、`MXQ` (弈天平台)，并支持“所有文件”以兼容未知格式。
  - **读取与同步**：打开棋谱后，棋谱记录窗口加载完整招法列表，点击任意步可跳转局面。
  - **保存与导出**：本地对局或连线对局均可保存/导出为上述任意格式。
  - **数据整合**：引擎的评估分数、搜索深度等信息会作为注释写入棋谱步法中，供形势分析窗口绘制曲线。